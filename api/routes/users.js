/**
 * api/routes/users.js  [STUB — NOT ACTIVE]
 *
 * Future user profile & preferences system.
 * Wire into api/index.js when ready.
 *
 * Planned endpoints (all require auth):
 *   GET    /api/users/me              — fetch profile
 *   PUT    /api/users/me              — update display name, avatar, etc.
 *   GET    /api/users/me/settings     — fetch app settings (eq, theme, etc.)
 *   PUT    /api/users/me/settings     — persist app settings to cloud
 *   DELETE /api/users/me              — delete account + all data (GDPR)
 */

'use strict';

// Stub — see architecture notes in api/routes/auth.js
